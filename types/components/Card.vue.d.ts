import { Vue } from "vue-property-decorator";
export default class Card extends Vue {
  private id;
  private variant;
  private img;
  private title;
  private subtitle;
  private description;
  private action;
  private element;
  mounted(): void;
}
